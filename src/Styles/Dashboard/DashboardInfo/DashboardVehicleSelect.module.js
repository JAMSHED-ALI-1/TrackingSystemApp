import { StyleSheet } from "react-native";
import Theme from "../../../Theme/theme";

const dash_select_stlyes = StyleSheet.create({

    dash_text:{
        color: '#303030',
        fontSize: 20, 
        fontWeight : Theme.font.xbold, 
    }

});

export default dash_select_stlyes;