import { StyleSheet } from "react-native";

const driver_add_styles = StyleSheet.create({
    driver_add_box: {
      justifyContent: 'center',
      alignItems: 'center',
      height: '80%',
      width: '100%',
    },
    driver_add_child_box: {
      borderColor: '#D7D7D7',
      borderWidth: 1,
      width: 71,
      height: 32,
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: 'center',
      borderRadius: 5,
      justifyContent: 'space-evenly',
    },
    driver_add_text: {
      color: '#484848',
      fontSize: 12,
      fontWeight: '600',
    },
  });
export default driver_add_styles  