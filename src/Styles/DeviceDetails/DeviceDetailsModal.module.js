import { StyleSheet } from 'react-native'

const devive_modal_styles = StyleSheet.create({
  device_modal_linear: {
    // borderWidth: 1,
    // borderColor: 'red',
    height: 40,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 6,
  },

  linear_text: {
    color: '#FFFFFF',
    fontSize: 18,
    fontWeight:'700'
  },
})

export default devive_modal_styles
