import { StyleSheet } from "react-native";

const custom_styles = StyleSheet.create({
    custom_main_box:{ width: '95%', alignSelf: 'center' },
    custom_select:{ flexDirection: 'row', justifyContent: 'space-between' },
    history_slider:{
        height:150 
    }

});

export default custom_styles;