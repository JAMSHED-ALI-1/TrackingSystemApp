import { StyleSheet } from "react-native";

const distance_report_styles = StyleSheet.create({

    distance_main_box: {
        width: '97%',
        alignSelf: 'center',
        marginVertical: 15,
        // borderWidth:1,
    },
    vehicle_select_box: {
        height: 35,
        marginTop: 10,
        // borderWidth:1,
        marginBottom: 15,
    }
});

export default distance_report_styles;