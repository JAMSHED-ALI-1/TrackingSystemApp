import { StyleSheet } from 'react-native'

const daily_report_styles = StyleSheet.create({
  vehicle_select_box: { height: '5%', margin: 10 },
  toggle_btn_box:{
    marginTop:30,
    // borderWidth:1,
  }
});
export default daily_report_styles;
