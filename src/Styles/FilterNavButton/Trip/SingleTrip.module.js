import { StyleSheet } from 'react-native'

const single_trip_styles = StyleSheet.create({
  details_box: {
    // borderWidth:1,
    width: '25%',
    // backgroundColor:'teal'
  },
  update_box: {
    marginLeft: -35,
    marginTop:-10,
   
    // borderWidth: 1,
    width:'25%'
  },
})

export default single_trip_styles